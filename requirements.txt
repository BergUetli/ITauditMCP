# =============================================================
# IT Audit MCP Server - Dependencies
# =============================================================
# Install with: pip install -r requirements.txt
# =============================================================

# --- MCP Server Framework ---
# FastMCP is the Python SDK for building MCP servers.
# It auto-generates tool schemas from Python type hints.
# We use v3+ which supports Streamable HTTP transport.
fastmcp>=3.0

# --- Database ---
# Supabase Python client for talking to our PostgreSQL database.
# All our framework data, controls, mappings live in Supabase.
supabase>=2.0

# --- LLM Providers ---
# Anthropic SDK (Claude) - our primary LLM for the analyst role
anthropic>=0.40

# OpenAI SDK - supports both OpenAI and Azure OpenAI
# Used for the reviewer role (or as an alternative analyst)
openai>=1.50

# --- Configuration ---
# Pydantic for settings management and data validation.
# Our models (Control, Framework, etc.) are all Pydantic models.
pydantic>=2.0
pydantic-settings>=2.0

# --- Utilities ---
# python-dotenv loads our .env file (API keys, DB URLs, etc.)
python-dotenv>=1.0
